"""Arquimedes MCP server — exposes the tutor's tools over the Model
Context Protocol so any MCP client (Claude Desktop, Cursor, Cline) can
consume them with the same schemas the LangGraph agent uses.
"""
