# LangGraph core
langgraph>=0.2.0
langgraph-prebuilt>=0.1.0
langgraph-checkpoint-sqlite>=1.0.0
langchain-core>=0.3.0

# LLM provider integrations (LangChain)
langchain-openai>=0.2.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0

# Provider SDKs (direct)
openai>=1.50.0
anthropic>=0.39.0
google-generativeai>=0.8.0
huggingface-hub>=0.25.0

# Observability
langsmith>=0.1.0

# API + UI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gradio>=5.0.0,<6.0.0
sse-starlette>=2.0.0

# Utilities
httpx>=0.27.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# RAG stack
chromadb>=0.5.0
langchain-chroma>=0.1.0
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
pypdf>=4.0.0

# Math tools (symbolic + plotting)
sympy>=1.13.0
matplotlib>=3.9.0
numpy>=1.26.0

# Async SQLite (optional persistence for API path)
aiosqlite>=0.20.0

# Fine-tuning solver (optional — Inference API mode only needs huggingface_hub,
# local GPU mode additionally needs torch + transformers + peft + bitsandbytes
# which are NOT pinned here to keep the prod image slim; see
# arquimedes/finetuning/README.md for a CUDA install command).
datasets>=2.19.0

# MCP (Model Context Protocol) — exposes Arquimedes tools to Claude Desktop,
# Cursor, Cline, etc.
mcp>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
